server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/munawarhassan/currencyconfigdetails.git
          skip-ssl-validation: true 
management:
  endpoints:
    web:
      exposure:
        include: '*'  
